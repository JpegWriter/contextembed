/**
 * Alt Text Provider Module
 *
 * Deterministic alt-text generation with structured output,
 * Zod validation, retry, and safe fallback.
 */

export { AltTextGenerator, createAltTextGenerator } from './generator';
